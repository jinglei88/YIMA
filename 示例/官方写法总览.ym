# 易码官方写法总览（可直接运行）

显示 "=== 官方写法总览 ==="

# 1) 赋值 + 列表 + 遍历
数字们 = [1, 2, 3, 4]
总和 = 0
遍历 数字们 里的每一个 叫做 当前
    总和 += 当前
显示 "总和是："
显示 总和

# 2) 条件
如果 总和 >= 10 就
    显示 "总和达到 10 及以上"
不然
    显示 "总和小于 10"

# 3) 函数
功能 平方(x)
    返回 x * x

显示 "5 的平方是："
显示 平方(5)

# 4) 异常处理
尝试
    内容 = 读文件("一个不存在的文件.txt")
如果出错 叫做 错误
    显示 "捕获到错误："
    显示 错误

# 5) 模块导入
引入 "math" 叫做 数学库
显示 "sqrt(16) ="
显示 数学库.sqrt(16)

用 "math" 中的 pi
显示 "pi ="
显示 pi

显示 "=== 运行结束 ==="
