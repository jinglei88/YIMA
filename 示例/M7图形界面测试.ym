# 易码图形界面大测试

我的窗口 = 建窗口("易码打招呼软件", 400, 300)

加文字(我的窗口, "欢迎来到易码的图形世界！")
加文字(我的窗口, "请输入你的名字：")

名字输入框 = 加输入框(我的窗口)

# 创建一个当按钮按下去时要执行的功能
功能 问候一下 
    填写的名字 = 读输入(名字输入框)
    如果 填写的名字 == "" 就
        弹窗("警告", "名字不能为空哦！")
    不然
        问候语 = "你好呀，" + 填写的名字 + "！"
        弹窗("问候", 问候语)

加文字(我的窗口, "点击下面按钮试试看：")
加按钮(我的窗口, "点我打招呼", "问候一下")

# 最后，展示出这个窗口（程序会停在这里等用户操作）
打开界面(我的窗口)
