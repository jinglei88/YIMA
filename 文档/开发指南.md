# 易码开发指南（用户版，真实实现）

本指南面向“要用易码开发真实软件”的用户，不讲编译器内部，只讲可直接落地的做法。

## 1. 你现在可以做什么

当前实现已经可覆盖以下软件类型：

1. 命令行工具：文本处理、数据转换、批量脚本。
2. 本地数据应用：文件读写、JSON、SQLite（事务）。
3. 联网工具：GET/POST、JSON API 对接。
4. 图形化小工具：窗口、输入框、按钮、网格表格。
5. 画板/小游戏：基于 turtle 的画图、键盘监听与交互。

## 2. 环境与启动

要求：

1. Python 3.9+
2. 源码 UTF-8 编码

启动：

```bash
python 易码.py
python 易码.py 示例/欢迎.ym
python 易码编辑器.py
```

## 3. 推荐项目结构

建议一开始就按“分层”来写，后续维护更稳：

```text
你的项目/
├── 主程序.ym
├── 业务层.ym
├── 数据层.ym
├── 界面层.ym
└── 资源/
    └── 图标.ico
```

典型入口：

```ym
引入 "界面层" 叫做 界面
界面.运行()
```

## 4. 模块导入规则（真实行为）

### 4.1 两种导入

1. 整体导入：`引入 "模块名" 叫做 别名`
2. 精确导入：`用 "模块名" 中的 名称`

### 4.2 模块查找顺序

运行时导入顺序：

1. 内置虚拟模块（如 `图形界面`、`本地数据库`）。
2. 本地 `.ym` 模块（当前工作目录及 `示例/`）。
3. Python 模块（如 `math`、`time`）。

### 4.3 精确导入建议

1. 本地模块建议写相对名：`用 "数据层.ym" 中的 查询用户`
2. 内置模块建议只导入你用到的命令，减少命名污染。
3. 若提示未定义，优先检查：模块名、名称拼写、全角/半角。

## 5. 开发软件的推荐流程

1. 先做数据层：先把文件/数据库读写跑通。
2. 再做业务层：把规则封装成 `功能`。
3. 最后接界面层：按钮只负责调用业务功能。
4. 每完成一个功能就运行，别堆大改动再调试。
5. 交付前跑自动回归，最后再走一遍手工交互。

### 5.1 什么时候需要改解释器

只有这类需求才需要改 `yima/解释器.py`：

1. 新语法关键字（例如新增一种循环语句）。
2. 新内置函数（例如新增系统能力入口）。
3. 解释器级 bug 修复（语言行为本身错误）。

下面这些都不需要改解释器：

1. 业务软件逻辑（应写在 `.ym` 项目文件）。
2. 项目模块拆分（`引入` / `用`）。
3. 使用 Python 生态库（通过 `引入 "模块名"`）。

### 5.2 打包原则（避免“写完还要改内核”）

当前打包器会自动做两件事：

1. 递归打包项目目录下的 `.ym` 文件（保留目录结构）。
2. 扫描 `.ym` 中 `引入/用` 语句，自动追加 Python `hidden-import`。

建议：把一个软件的源码放在独立项目目录内，再从该目录主入口打包。

## 6. 调试与定位

编辑器里重点看两个位置：

1. 右下角语法状态：语法是否通过。
2. 左侧“问题列表”：
   - `[错]` 语法/运行前错误定位
   - `[提]` 一般语义提示
   - `[精引]` 精确引入专项提示（模块名/名称不匹配）

调试控制台用于看：

1. 运行日志
2. 错误行号
3. 打包过程输出

## 7. 打包 EXE（编辑器）

推荐步骤：

1. 先保存全部文件。
2. 在编辑器点击“导出软件(EXE)”。
3. 先用“代码黑框版（推荐调试）”打一次，确认功能。
4. 再打“纯净窗口版（发布给用户）”。
5. 若有图标，填写 `.ico` 路径。

常见失败点：

1. 模块文件不在项目目录或导入路径里。
2. 打包名和输出路径不一致。
3. 资源文件未随项目保存。

### 7.1 打包清单（可选，推荐）

当自动依赖识别不够时，可在项目根目录放 `易码打包清单.json`（或 `yima_pack.json`）：

```json
{
  "hidden_imports": ["requests", "PIL.Image"],
  "datas": [
    "assets/logo.png",
    { "src": "assets", "dest": "assets" }
  ]
}
```

说明：

1. `hidden_imports`：手动补充 Python 依赖。
2. `datas`：附加资源文件/目录到 EXE。
3. `src` 支持相对项目根目录路径。
4. 可直接参考模板：`示例/易码打包清单_模板.json`。

## 8. 自动回归与手动回归

自动回归（推荐每次发版前执行）：

```bash
python 易码.py 示例/M16全量能力测试项目/主程序.ym
```

手动交互回归（覆盖阻塞命令）：

```bash
python 易码.py 示例/M16全量能力测试项目/07_GUI手动交互验证.ym
python 易码.py 示例/M16全量能力测试项目/08_画板手动交互验证.ym
```

发布前一键回归（基础回归 + M16 + 打包冒烟）：

```bash
python scripts/run_release_regression.py
```

若本次不需要跑打包冒烟，可用：

```bash
python scripts/run_release_regression.py --skip-pack
```

## 9. 高概率踩坑清单

1. `绑定按键` 第一个参数要传“功能对象”，不要传字符串。
2. `打开界面` 和 `定格` 会阻塞；自动测试不要放这两个命令。
3. `弹窗输入` 返回空字符串时要做兜底处理。
4. 数据库事务要配对：`开始事务 -> 提交事务/回滚事务`。
5. 语义提示不阻止运行，但建议修掉再发布。

## 10. 文档索引

1. `文档/速查表.md`：新手最快上手
2. `文档/语言规范.md`：完整语法与内置函数清单（101 个）
3. `文档/架构设计.md`：实现结构
4. `文档/开发者教程.md`：贡献者和底层开发
